from __future__ import annotations

TARGET_HORIZON_DAYS = 30
TARGET_MIN_FUTURE_MAGNITUDE = 4.5
TARGET_DEFINITION = (
    "target=1 when a spatial window has at least one future event "
    "within 30 days with magnitude >= 4.5"
)
